# Iteration 4.5 显式义务映射

## TDD 证据

- RED→GREEN：协议模型、`EvidenceMapping` 加载、config `mapping` 解析。
- RED→GREEN：`tests/engine/test_mapping.py` 覆盖 junit 翻译、MAP-001/002/003/005/006/007、
  playwright flaky→failed。
- RED→GREEN：`tests/cli/test_map.py` 覆盖 preview、合并评估、无 mapping 回归、直接路径不映射、
  policy `mappingDiagnostics`、空 diagnostics Markdown 章节。
- RED→GREEN：`scan_project` 保留 `records`；inventory 复用 `scan_adapter_files`。

## Review 修复

- 双语 `mapping.md` heading 对齐；README 改为 Features / Installation / Quick start /
  Usage / Documentation / Contributing / License。
- `QCOV-MAP-002` 仅用于 evidence id；文档 id 冲突改为 `QCOV-MAP-007`。
- scan diagnostic 使用 `artifactPath` 字段，不再塞进 `identity`。
- CLI 抽取 `_require_mapping_files`；应用 mapping 时 Markdown 始终输出 diagnostics 节。

## 最终验证

2026-09-08：

```text
/tmp/qcov-venv/bin/python -m pytest \
  --ignore=tests/engine/test_git_snapshots.py \
  --ignore=tests/cli/test_diff.py \
  --ignore=tests/examples/test_pr_delta_demo.py -q
# 87 passed（非 git 套件；含本迭代新增用例）

/tmp/qcov-venv/bin/ruff check .     # passed
/tmp/qcov-venv/bin/mypy qcov        # Success
/tmp/qcov-venv/bin/python -m build  # qcov-0.3.0 sdist + wheel
git diff --check                    # passed
```

依赖临时 git 仓库的用例在本代理沙箱中因禁止创建 `.git` 未能执行；请本地重跑全量
`pytest`。实现未改动 `diff` / snapshot 语义。

## 真实接入观察（本仓库 imported-reports）

- `map preview` 列出 junit/playwright 映射产物。
- `gaps --config` 在手写证据外观测到 `QE-MAP-junit-…-api_test`；并发/幂等/生产仍 MISSING。
- 接入约数分钟：两条精确 mapping，无需改测试代码。

## 范围

已交付声明式 mapping、`map preview`、`--config` 合并、scan records 保留、配对文档与
标准结构 README。未做：coverage/LCOV→passed、pytest marker 自动进 gaps、`diff` 感知
mapping、identity glob。
