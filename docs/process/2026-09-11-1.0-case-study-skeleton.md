# 1.0 真实项目案例骨架

## 范围

在本仓库新增三个只含元数据、显式 obligation 和离线复现说明的案例包：
`ai-native-qa-agents`（Python pytest coverage）、`ai4se-demo-project`
（Java JUnit JaCoCo）与 `naodeng.com.cn`（TypeScript Playwright）。未修改
任何外部项目，也没有复制其源码、测试结果或敏感配置。

## TDD 记录

- RED：`.venv/bin/pytest tests/examples/test_case_studies.py -q`，失败原因是
  `examples/case-studies/ai-native-qa-agents/metadata.yaml` 尚不存在。
- GREEN：建立三个案例目录、metadata、可加载的 obligation、离线 evidence 边界说明
  与双语公开说明后，重新执行相同测试。
- 第二个 RED：结构测试加入 QCovConfig 加载断言后，因三个 `qcov.yaml` 缺少
  `apiVersion` 与 `kind` 而失败。
- 第二个 GREEN：补齐三个配置的协议头后，结构测试通过。

## 验证

- `.venv/bin/pytest tests/examples/test_case_studies.py -q`
- `git diff --check`

## 未满足的 Release Gate

三个项目均未运行；每个项目目前仅有一个经审阅 obligation，不足 10 条；尚无
脱敏报告的导入与显式映射。Java 案例固定提交的 `backend/pom.xml` 也尚未配置
JaCoCo。故案例状态保持 `NOT_RUN`，Release Gate 保持 `NOT_MET`。
