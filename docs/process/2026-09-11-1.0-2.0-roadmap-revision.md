# QCov 1.0 → 1.5 → 2.0 路线图修订

## 范围

本次仅更新路线图、需求、迭代 8 的后续表述、工程规划记录与文档回归断言；不修改 Python 行为、协议实现或包版本（当前仍为 `0.8.0`）。已有未跟踪的 Iteration 8 设计与实施计划保留，并只改动其中与新路线直接冲突的路线图措辞。

设计依据：`docs/superpowers/specs/2026-09-11-1.0-2.0-roadmap-revision.md`
实施计划：`docs/superpowers/plans/2026-09-11-1.0-2.0-roadmap-revision.md`

## 决策记录

- Iteration 8 是 QCov 1.0 前的离线 production-observation 基础，不再写为路线图收官。
- Iteration 9 开启 QCov 1.0：真实项目验证、确定性的 Change → Obligation Impact、Adapter SDK v1 与协议稳定性。
- QCov 1.5 才进入 AI-assisted 理解与规划；QCov 2.0 才设计 Agent 闭环、`qcov.agent/v2`、QA for AI、生产反馈与 Q-BOM。
- AI、Adapter 与 Agent 均不是证据或门禁权威；Agent 不能自行标记 `COVERED`。

## TDD 与验证证据

### RED

执行：

```text
pytest tests/docs/test_documentation_links.py::test_roadmaps_define_the_1_0_to_2_0_evolution -q
```

结果：`1 failed`。失败原因为旧英文路线图不含 `Iteration 9`。

### GREEN

执行：

```text
pytest tests/docs/test_documentation_links.py -q
```

结果：`5 passed in 0.02s`。

执行：

```text
git diff --check
```

结果：通过，无空白错误。

未运行 `ruff`、`mypy`、`build` 或完整 `pytest`：本次没有 Python 行为、类型、打包或运行时变更；已运行覆盖本次路线图契约的文档测试。
