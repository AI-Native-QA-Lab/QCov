# v0.2.1 Release Record

## Scope

Iteration 1.1 releases the remediation for the code-review findings as QCov
`0.2.1`.

## TDD version evidence

The package version assertion was changed to the requested `0.2.1` before the
runtime and build metadata were updated:

```text
.venv/bin/pytest tests/test_package.py -q
# 1 failed: runtime version was 0.1.0a0, expected 0.2.1
```

After updating `qcov.__version__` and `pyproject.toml`, the focused version
test passed.

## Release verification

On 2026-09-06 (Asia/Shanghai), the candidate passed:

```text
.venv/bin/pytest tests/test_package.py -q  # 1 passed
.venv/bin/pytest -q                        # 40 passed
.venv/bin/ruff check .                     # passed
.venv/bin/mypy qcov                        # 20 source files, no issues
.venv/bin/python -m build                  # qcov-0.2.1.tar.gz and qcov-0.2.1-py3-none-any.whl built
git diff --check                            # passed
```

## Release contents

The paired changelogs and GitHub Release notes describe the same review
remediation: corrected `UNKNOWN` semantics, valid starter configuration,
independent config overrides, explainable report facts, bounded pytest
discovery, adapter/path-contract cleanup, and synchronized documentation.
