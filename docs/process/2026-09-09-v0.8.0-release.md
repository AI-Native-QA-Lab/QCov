# v0.8.0 Release Record

## Scope

发布 Iteration 7：Agentic Quality Loop 助手面（`qcov explain`、`qcov agent next`、
可选 `qcov agent validate-evidence`），稳定 `contractVersion: qcov.agent/v1`。

## TDD version evidence

先将 `tests/test_package.py` 期望改为 `0.8.0`，观察到 RED（当时仍为 `0.6.0`）。
再更新 `qcov.__version__` 与 `pyproject.toml` 后 GREEN。

## Release validation

2026-09-09 实际执行：

```text
.venv/bin/pytest -q
176 passed in 15.78s

.venv/bin/ruff check .
All checks passed!

.venv/bin/mypy qcov
Success: no issues found in 42 source files

.venv/bin/python -m build
Successfully built qcov-0.8.0.tar.gz and qcov-0.8.0-py3-none-any.whl

git diff --check
(exit 0)
```

Release notes 取自 `CHANGELOG.md` 的 `0.8.0` 节；GitHub Release 发布到
`naodeng/QCov` 与上游 `AI-Native-QA-Lab/QCov`。
