# Iteration 4.5 显式义务映射设计

## 范围

新增设计规格：
`docs/superpowers/specs/2026-09-08-iteration-4.5-obligation-mapping-design.md`。

无代码行为变更。自审已吸收：Playwright identity 与 adapter 对齐、缺省
`evidenceId` 纳入 obligationRef/type、artifact 相对化、mapping-only 配置门槛、
diagnostics 表面化、歧义匹配 `QCOV-MAP-005`、非法 producer 归入
`QCOV-SCHEMA-001`，以及关闭迭代时的真实接入验证过程交付。

## 验证

2026-09-08：仅新增 Markdown 设计稿。提交前执行 `git diff --check`。
