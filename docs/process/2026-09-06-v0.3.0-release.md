# v0.3.0 Release Record

## Scope

This release publishes Iteration 2: local Playwright JSON and LCOV inventory
readers, deterministic built-in registration, configuration support, fixtures,
and paired documentation.

## TDD version evidence

The package version test was changed to require `0.3.0` before production
version files changed. It failed because the runtime still reported `0.2.1`.
After updating `qcov.__version__` and `pyproject.toml`, the focused test passed.

## Release validation

Run the package tests, static checks, isolated build, example scan, and diff
check before tagging. Release notes come from `CHANGELOG.md` version `0.3.0`.
