# Iteration 1 Project Configuration Record

## Scope

Task 1 adds the strict `QCovConfig` v1alpha1 model, local config loader,
relative glob resolver, and public JSON Schema. It also formalizes the RED →
GREEN TDD requirement in `AGENTS.md`.

## TDD evidence

Before implementation, `tests/models/test_config.py` failed during collection
with `ModuleNotFoundError: No module named 'qcov.models.config'`.

After the minimal model/loader/resolver implementation, 2026-09-04
(Asia/Shanghai) verification passed:

```text
.venv/bin/python -m pytest tests/models/test_config.py -q  # 3 passed
.venv/bin/ruff check qcov/models tests/models              # passed
.venv/bin/mypy qcov/models                                  # passed
python -m json.tool schemas/config.schema.json              # passed
git diff --check                                             # passed
```
