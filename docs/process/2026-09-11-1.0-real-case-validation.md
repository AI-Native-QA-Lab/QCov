# QCov 1.0 三个真实案例验证记录

## 范围与边界

三个目标项目均从指定 SHA 创建临时克隆，未在原项目目录安装依赖、执行测试或写入
报告。QCov 新增内容仅位于 `examples/case-studies/`、`docs/case-studies/` 与本文件。
工件只保留 test identity、状态及最小聚合 inventory；已移除源码、错误堆栈、绝对路径、
主机名、环境变量、数据库参数和业务载荷。

## TDD / RED-GREEN 记录

本轮没有变更 QCov 生产代码。案例骨架的历史 RED/GREEN 已记录在
`2026-09-11-1.0-case-study-skeleton.md`：先因缺少案例 metadata 与协议头失败，再在建立
案例包和协议头后通过。当前补充的是运行产物、映射和状态数据；相应结构测试更新为接受
`SOURCE_DERIVED_REVIEWED_PENDING_OWNER_ACCEPTANCE` 并要求 reviewer、日期和结论。
本轮未伪造一个事后 RED；后续若把案例数据校验提升为新的行为契约，必须先记录新的 RED。

## 固定提交与实际运行

| 案例 | 固定提交 | 实际命令结果 | 可再分发工件 |
| --- | --- | --- | --- |
| ai-native-qa-agents | `e3472a82dd3c090bd1b0cbd17a2e1efef6098566` | `PYTHONPATH=packages python -m pytest -q --junitxml=... --cov=qa_agent --cov-report=xml:...`：90 passed，13 warnings | `pytest-junit.xml`（90 identity）、`coverage.xml`（1 条 inventory） |
| ai4se-demo-project | `e02408b92a0fb085f8ac0bb72f0d010c4bbbe809` | `mvn ... prepare-agent test ... report`：退出码 1，18 errors；根因是测试数据库 connection refused | `junit.xml`（18 failed identity）、`jacoco.xml`（1 条 inventory） |
| naodeng.com.cn | `31250a6a4bfc5620aee671224e698a655b58eda7` | `npm ci`、Playwright Chromium 安装和 `npm run build` 成功；E2E 预览监听被沙箱以 `listen EPERM` 拒绝 | `playwright.json`（BLOCKED、0 identity） |

Java 与 Playwright 均记为 `BLOCKED`，没有被计入通过。Python 测试告警也没有被隐去，
但其退出码为 0。

## 审阅、导入与 QCov 结果

每个案例的 10 条 obligation 已逐条审阅 `source.ref`、标题、风险和 `requiredEvidence`。
状态为 `SOURCE_DERIVED_REVIEWED_PENDING_OWNER_ACCEPTANCE`，不是项目所有者的批准。

| 案例 | scan | map preview | gaps（逐 obligation） | impact / affected |
| --- | ---: | ---: | --- | --- |
| Python | JUnit 90；coverage.py 1 | 20 passed evidence，0 diagnostic | 10 COVERED | `requirements.py` → QO-CASE-PY-001；affected 无 key gap |
| Java | JUnit 18；JaCoCo 1 | 17 failed evidence，0 diagnostic | 8 MISSING，2 UNKNOWN | conflict service → QO-CASE-JAVA-001；3 个缺失维度 |
| TypeScript | Playwright 0 | 0 evidence，18 个 QCOV-MAP-001 | 10 UNKNOWN | navigation spec → 全部 10；均为 key gap |

所有 `scan`、`map preview`、`impact` 与 `affected` 命令退出码均为 0。`gaps` 必须逐个
obligation 调用（配置含多个 obligation 时会返回 `QCOV-CONFIG-001`）；30 次逐项调用均为
0。impact 使用显式 `--changed-file` 做映射演示，并不声称固定提交之间存在真实变更。

## TTFV 与采用指标

TTFV 从干净临时 Python 虚拟环境创建、安装本地 QCov 到首次成功输出 `qcov gaps` 计时：
Python 12 秒、Java 7 秒、TypeScript 7 秒。该测量使用各自独立环境和同一案例配置。

- false-gap rate：`UNASSESSED`；没有项目所有者或独立开发/QA 对抽样 gap 的裁决，分母为 0。
- gap → added-verification conversion：`0 / 20 = 0%`；本验证窗口未修改目标项目，也没有新增
  验证，20 指 Java/Playwright 的非 COVERED obligation。
- 独立开发/QA 结论：`0 / 2`，未收集；不得用 agent 结论替代独立人员反馈。

## Release Gate

**NOT_MET**。缺少两名独立反馈、所有者接受、Java 可用测试数据库、允许本地监听的
Playwright 运行环境，以及可判定的 false-gap 样本和至少一个 gap→新增验证转化。Python
的 10 个 COVERED 结果不抵消其他案例的 BLOCKED/UNKNOWN/MISSING 状态。

## QCov 最终质量门

在案例工件完成脱敏后执行：

| 命令 | 退出码 | 结果 |
| --- | ---: | --- |
| `PYTHONPATH=. ../../.venv/bin/pytest -q` | 0 | 208 passed in 15.13s |
| `../../.venv/bin/ruff check .` | 0 | All checks passed |
| `../../.venv/bin/mypy qcov` | 0 | Success: no issues found in 48 source files |
| `../../.venv/bin/python -m build` | 0 | built `qcov-0.9.0.tar.gz` 与 `qcov-0.9.0-py3-none-any.whl` |
| `git diff --check` | 0 | 无空白错误 |

初次无网络 build 不能获取 `hatchling`，在允许的隔离构建依赖下载后重试通过；这不是
产品测试通过的替代证据。最终还重跑了 Python case `scan`、`map preview`、案例结构测试
和 `git diff --check`；Python mapping 仍为 20 条 evidence、0 条 diagnostic。

## 后续验证与接受记录（2026-09-12）

上文的 BLOCKED、待所有者接受和缺少独立结论是 2026-09-11 的历史快照，不代表当前状态。
项目所有者后来接受三个案例的 30 条 obligation，且已收集独立 QA 与开发审阅结论。

Java 案例已在本地固定提交 `c7d19b48433e9794c58b2c498201bd1181d4c99a` 上使用 JDK 19
运行：23 tests、0 failures、0 errors、0 skipped，并生成 JaCoCo XML。coverage/JaCoCo 仍
只作为 inventory；业务证据必须来自显式 JUnit mapping。Release Gate 继续为 `NOT_MET`，仅因
false-gap 结果需要按修复后的 mapping 和最新报告重新裁决。
