# QCov 1.0（Iteration 9）路线图与上手交付修订

## 范围

以已发布 `v0.9.0` 为基线，确认 Iteration 8 已交付，并把 Iteration 9 设为 QCov 1.0 主线。此次改动仅更新路线图、需求、工程约束、规划记录与文档断言；不修改协议、运行时代码或版本号。

## 核验事实

- Iteration 8 实现提交：`ed1d592 feat: add production observation evidence`。
- PR #18 已合并，发布提交：`04ee040 chore: release v0.9.0`。
- `v0.9.0` 基线全量测试：`PYTHONPATH=. ../../.venv/bin/pytest -q`，结果 `185 passed in 12.39s`。

## 决策

- QCov 1.0 的 Iteration 9 聚焦真实项目验证、确定性 Change → Obligation Impact、Adapter SDK v1 与协议稳定性。
- README 清理、安装、快速开始、使用、配置、示例与故障排查是 1.0 的正式门槛；双语公开文档必须以真实命令验证。
- 1.5 和 2.0 的 AI/Agent 能力仍需单独设计，且不能成为 evidence 或 gate authority。

## TDD 与验证

新增路线图断言后执行：

```text
PYTHONPATH=. ../../.venv/bin/pytest tests/docs/test_documentation_links.py::test_roadmaps_start_qcov_1_0_after_delivered_iteration_8 -q
```

首次结果为 RED：旧路线图缺少 `Iteration 9`。在双语路线图中加入 1.0 主线及 Documentation and onboarding 后，因中文标题断言不匹配出现一次 RED；修正中英文标题断言后执行：

```text
PYTHONPATH=. ../../.venv/bin/pytest tests/docs/test_documentation_links.py -q
git diff --check
```

结果：`6 passed in 0.01s`，且无空白错误。

未重新运行 ruff、mypy、build：本次仅为 Markdown 与文档测试变更；全量 pytest 已在本次隔离基线中通过。

## 运行时与文档实现收尾

本轮已实现 `qcov impact`、`qcov affected`、`qcov.impact/v1` schema、
`qcov.adapter/v1` SDK 与 JaCoCo inventory adapter，并增加双语 README、五分钟上手
与真实项目案例输入包。实际案例命令结果记录在
`docs/process/2026-09-11-1.0-case-study-skeleton.md`。

最终验证（2026-09-11）：

```text
PYTHONPATH=. ../../.venv/bin/pytest -q  -> 208 passed in 14.20s
../../.venv/bin/ruff check .            -> All checks passed
../../.venv/bin/mypy qcov               -> Success: no issues found in 48 source files
git diff --check                         -> passed
../../.venv/bin/python -m build         -> passed; built qcov-0.9.0.tar.gz and qcov-0.9.0-py3-none-any.whl
```

此前受限环境的 PyPI 阻塞已在可联网环境中解除，构建现已通过。版本号仍为现有
`0.9.0`，本记录不将此构建误称为 1.0 发布。其余 Release Gate 指标仍需如实完成。

## 多 obligation 案例回归

案例配置以 `obligations/*.yaml` 声明每项目十条 obligation。修复前 `qcov gaps --config`
只渲染第一条结果；现已改为对配置解析出的全部 obligation 评估并渲染。验证结果：三个
案例各输出 10 条结果（总计 30 条），全量质量门为 `208 passed`、Ruff 和 mypy 通过，且
`git diff --check` 通过。该能力只评估显式配置；未映射或受阻的真实项目证据仍保持缺口，
不能据此将 Release Gate 标为通过。

## 项目负责人和开发侧验收

2026-09-11 收到项目负责人对修订后 30 条 obligation 的最终 `ACCEPT`，并按授权将
三个案例 metadata 更新为 `OWNER_ACCEPTED`。同日收到全栈开发工程师独立验收：可解释性
高、行动价值高、开发侧结论 `ACCEPT`，确认 QCov 能区分测试执行/覆盖率与业务 obligation
证据。

指标记录：Python TTFV 12 秒；Java TTFV 7 秒；TypeScript TTFV 7 秒。Java 与
TypeScript 各抽样 10 条 Gap，当前均为可行动但没有新增验证转化（0%）；Python 没有可
抽样 Gap。三项目 false-gap rate 均为 `UNASSESSED`，因为尚无逐项误报裁决；运行时版本和
TTFV 精确起止时间缺少记录的项目保持 `UNASSESSED`。这些指标不替代报告导入、mapping
或最终发布审查。

## 复审修订

第二轮设计复审发现并修正：

- 将 `docs/superpowers/plans/` 计划正文改为中文，保留命令和协议标识的英文拼写；
- 为 `qcov.impact/v1` 固定 Gap 项、diagnostics、排序、状态转换、退出码和互斥输入语义；
- 新增 `schemas/qcov.impact-v1.schema.json`，并在双语协议文档中登记 `qcov.impact/v1` 与 `qcov.adapter/v1`；
- 明确 `ai4se-demo-project`、`ai-native-qa-agents` 与 `naodeng.com.cn` 为固定必需案例，目标项目只读；
- 将 1.5、2.0 路线图措辞收窄为“候选主题，需另行批准设计”。

复审后执行：

```text
PYTHONPATH=. ../../.venv/bin/pytest tests/docs/test_documentation_links.py -q
PYTHONPATH=. ../../.venv/bin/pytest -q
../../.venv/bin/ruff check .
../../.venv/bin/mypy qcov
python3 -m json.tool schemas/qcov.impact-v1.schema.json
git diff --check
```

结果：文档测试 `7 passed`，全量测试 `187 passed in 11.02s`，ruff 和 mypy 通过，Impact Schema 可解析，`git diff --check` 通过。`python -m build` 因隔离环境无法访问 PyPI 获取 `hatchling` 而阻塞，未将其计为通过。1.0 的真实项目案例、运行时 Impact/Adapter 实现、README 上手内容和 Release Gate 仍未完成，不能标记为 1.0 就绪。

## 最新案例证据核对（2026-09-11）

在用户提供的 Java 数据库与 Playwright 运行结果基础上，重新执行了两组 mapping：

- Java JUnit 显式命中全部 10 条业务 obligation（JAVA-001～010），均为 `passed`；其余测试仅作为执行清单，不提升为业务证据。
- Playwright 显式命中 TS-001 ～ TS-010 共 20 条维度证据，均为 `passed`；TS-010 的 identity 与“新标签页打开”语义一致。

验证命令：

```text
PYTHONPATH=. ../../.venv/bin/python -m qcov map preview --config examples/case-studies/ai4se-demo-project/qcov.yaml --format json
PYTHONPATH=. ../../.venv/bin/python -m qcov map preview --config examples/case-studies/naodeng.com.cn/qcov.yaml --format json
PYTHONPATH=. ../../.venv/bin/pytest tests/examples/test_case_studies.py tests/cli/test_map.py -q  -> 9 passed
../../.venv/bin/ruff check .                                      -> All checks passed
../../.venv/bin/mypy qcov                                         -> Success (48 files)
git diff --check                                                   -> passed
```

Java 10 条 obligation 均已有明确 JUnit 业务 mapping，JaCoCo 也已在 JDK 19 下无警告通过。项目负责人和开发侧验收已完成；Release Gate 仍需补齐三项目版本/TTFV 精确记录及 gap→verification 转化数据，因此暂保持 `NOT_MET`。

## 构建复核（2026-09-11）

用户在联网环境激活 QCov 虚拟环境并安装 `hatchling` 后执行 `python -m build` 成功，产物为
`qcov-0.9.0.tar.gz` 与 `qcov-0.9.0-py3-none-any.whl`。该结果覆盖了此前仅因沙箱无法访问 PyPI
导致的构建阻塞；版本仍为 0.9.0，不能据此宣称已发布 1.0。

## 证据复审修正（2026-09-12）

复审撤回了不能由测试断言直接证明的 Java mapping：仪表盘测试不能证明管理员确认预约，
可用时段测试不能证明预约时段边界，普通取消测试不能证明全部允许/拒绝状态。JUnit XML
仅保留为 `scan.junit` inventory 输入，不再放入 `evidence`。Java 新增测试和业务校验目前
仍是源项目工作树未提交改动，因此 metadata 的固定提交不能声称复现这些新证据；Release
Gate 保持 `NOT_MET`，直到源项目变更固定并重新导入报告。

随后源项目已在本地固定为 `c7d19b48433e9794c58b2c498201bd1181d4c99a`
（`test: cover appointment business rules`），并将测试数据库密码改为环境变量形式；Java
案例 metadata 已更新到该提交。固定提交问题已解除，但不改变 false-gap 复核与其他案例
所需的 Release Gate 判断。

## 运行时版本记录（2026-09-12）

- Python 案例：Python 3.14.7，pytest 7.4.3。
- Java 案例：Maven 3.9.16；JaCoCo 验证必须通过 `JAVA_HOME` 固定到 OpenJDK 19。当前 `mvn --version` 默认显示 Homebrew JDK 26，不能用于兼容性结论。
- Playwright 案例：Node.js 26.8.2、npm 11.19.1、Playwright 1.58.2。

TTFV 精确记录：Python 2026-09-12T08:38:39.716386+08:00 → 08:38:40.172991+08:00（约 0.46 秒）；Java 08:38:44.088010+08:00 → 08:38:44.323992+08:00（约 0.24 秒）；Playwright 08:38:48.273699+08:00 → 08:38:48.515317+08:00（约 0.24 秒）。

Gap → Added Verification：Java 新增的 JAVA-003、JAVA-004、JAVA-006、JAVA-010 四条 obligation 均已有对应测试且通过，转化为 4/4（100%）；其余项目本轮无新增 Gap 转化。三项目版本与 TTFV 已记录，Release Gate 可进入最终复核。
