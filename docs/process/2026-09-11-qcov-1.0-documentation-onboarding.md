# QCov 1.0 文档 onboarding 过程记录

## 范围

完成双语 README 与 `docs/en|zh-CN/getting-started.md` 的可执行 onboarding；覆盖安装、
`gaps`、`scan`、`map preview`、映射后的 `gaps`、本地 Git `diff` 与 `policy check`。不修改
CLI、协议、示例数据或外部项目。

计划：`docs/superpowers/plans/2026-09-11-qcov-1.0-documentation-onboarding.md`。

## TDD 记录

先扩展 `tests/docs/test_documentation_links.py` 的公开文档契约：要求两份 README 和两份
onboarding 页面共同覆盖当前流程、Python/Java/TypeScript 入口、coverage.py/LCOV 边界，且
onboarding 说明 `qcov impact` 与 `qcov affected` 尚未实现。

RED（2026-09-11）：

```text
.venv/bin/python -m pytest tests/docs/test_documentation_links.py -q
FAILED ... README.md must document Java
1 failed, 5 passed
```

新增配对 onboarding 页面和 README 链接后，第二次运行还暴露了语言文档 stem 清单未登记
`getting-started.md`，以及 wording 断言过度依赖单句字面。将该契约更新为检查双语页面的
明确“已实现命令”说明，而不是误把未实现命令名当作禁止出现的文本。

GREEN（2026-09-11）：

```text
.venv/bin/python -m pytest tests/docs/test_documentation_links.py -q
6 passed in 0.01s
```

## 可执行命令验证

同日逐条运行 onboarding 中的实际 CLI 路径：

```text
.venv/bin/python -m qcov gaps --obligation examples/refund/obligation.yaml --evidence examples/refund/evidence  -> exit 0
.venv/bin/python -m qcov scan --config examples/imported-reports/qcov.yaml                                  -> exit 0
.venv/bin/python -m qcov map preview --config examples/imported-reports/qcov.yaml --format json              -> exit 0
.venv/bin/python -m qcov gaps --config examples/imported-reports/qcov.yaml                                   -> exit 0
.venv/bin/python examples/pr-delta/demo.py                                                                    -> exit 0
.venv/bin/python -m qcov policy check --config examples/imported-reports/qcov.yaml --policy examples/refund/policy.yaml --as-of 2026-09-07T00:00:00+08:00  -> exit 0
```

验证期间发现 `examples/refund/qcov.yaml` 缺少当前 `ProjectConfig` 必需的 `apiVersion` 与
`kind`，将其传给 `--config` 会以 `QCOV-CONFIG-001` 退出。因此 README/onboarding 没有把它
作为公开命令；改用完整的 `examples/imported-reports/qcov.yaml` 与同一 Refund policy。

本次未运行 `ruff`、`mypy`、build 或完整 pytest：改动仅限 Markdown 与文档契约测试，没有
Python 运行时、类型或打包行为改动。
