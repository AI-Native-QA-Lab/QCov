# Contributing to QCov

Read `AGENTS.md`, the [development guide](docs/en/development.md), and the
active implementation plan before changing behavior. Use test-first development,
run the scoped checks plus `git diff --check`, and record implementation and
verification evidence in `docs/process/`. Update matching English/Chinese docs
when public behavior or terminology changes.
